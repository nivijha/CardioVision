# Frontend Vite env example
# Use VITE_ prefix for Vite to expose to client code
VITE_API_BASE_URL=https://cardiovision-bt72.onrender.com

# Optional: override the backend used by the dev server
BACKEND_URL=http://localhost:8000

# Note: Do NOT store secrets in frontend env: public variables are embedded in builds.
