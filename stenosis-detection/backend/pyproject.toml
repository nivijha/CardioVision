[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
# Pragmatic set: errors, unused imports, warnings, import sorting.
select = ["E", "F", "W", "I"]
ignore = ["E501"]
