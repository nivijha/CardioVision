fastapi==0.115.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pillow==10.4.0
numpy==1.26.4
# NOTE: Do NOT install torch via pip install -r requirements.txt on Windows.
# It installs the wrong build and causes fbgemm.dll errors.
# Instead, install manually AFTER the rest:
#   CPU-only:  pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   CUDA 12.1: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
torchvision>=0.15.0
ultralytics==8.2.50
opencv-python==4.10.0.84
scikit-image==0.24.0
aiofiles==24.1.0
pydantic==2.8.2
